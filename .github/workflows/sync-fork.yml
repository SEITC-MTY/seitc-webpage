# name: Sync Fork

# # Trigger whenever there is a push to the upstream repo
# on:
#   workflow_run:
#     workflows: ["Node.js build"]
#     types:
#       - completed

# jobs:
#   sync:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout fork
#         uses: actions/checkout@v4
#         with:
#           fetch-depth: 0

#       - name: Add upstream remote
#         run: |
#           git remote add upstream https://github.com/ORGANIZATION/UPSTREAM-REPO.git
#           git fetch upstream

#       - name: Merge upstream changes
#         run: |
#           git checkout main
#           git merge upstream/main --allow-unrelated-histories --no-edit

#       - name: Push changes to fork
#         env:
#           GH_TOKEN: ${{ secrets.GH_TOKEN }} # PAT with repo permissions
#         run: |
#           git push https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }}.git main
